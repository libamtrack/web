language: node_js
node_js:
  - "node"

install:
  - npm install
script:
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GH_TOKEN
  keep-history: true
  local-dir: ./dist
  on:
    all_branches: true
    condition: $TRAVIS_BRANCH =~ master|^feature$